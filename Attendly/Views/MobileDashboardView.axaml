<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Attendly.ViewModels"
             xmlns:controls="using:Attendly.Controls"
             x:Class="Attendly.Views.MobileDashboardView"
             x:DataType="vm:MobileDashboardViewModel">
  <Grid RowDefinitions="Auto,*" Background="{DynamicResource AppBackgroundBrush}">
    <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="16,16,16,4">
      <StackPanel Grid.Column="0">
        <TextBlock Text="Dashboard" Classes="heading"/>
        <TextBlock Text="{Binding TodayDisplay}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
      </StackPanel>
      <Button Grid.Column="1" Command="{Binding RefreshCommand}" Background="Transparent" Padding="6" VerticalAlignment="Center">
        <controls:LucideIcon Kind="RefreshCw" Width="18" Height="18"/>
      </Button>
    </Grid>

    <ScrollViewer Grid.Row="1">
      <StackPanel Margin="16,8,16,20" Spacing="14">

        <!-- Today's cross-Kelas summary -->
        <UniformGrid Columns="3" Rows="1">
          <Border Background="{DynamicResource SurfaceMutedBrush}" CornerRadius="16" Padding="10,14" Margin="0,0,6,0">
            <StackPanel Spacing="2" HorizontalAlignment="Center">
              <TextBlock Text="{Binding CountHadir}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource StatusSuccessBrush}" HorizontalAlignment="Center"/>
              <TextBlock Text="Hadir" FontSize="11" Foreground="{DynamicResource StatusSuccessBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
          <Border Background="{DynamicResource SurfaceMutedBrush}" CornerRadius="16" Padding="10,14" Margin="3,0">
            <StackPanel Spacing="2" HorizontalAlignment="Center">
              <TextBlock Text="{Binding CountAlpha}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource StatusErrorBrush}" HorizontalAlignment="Center"/>
              <TextBlock Text="Alpha" FontSize="11" Foreground="{DynamicResource StatusErrorBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
          <Border Background="{DynamicResource SurfaceMutedBrush}" CornerRadius="16" Padding="10,14" Margin="6,0,0,0">
            <StackPanel Spacing="2" HorizontalAlignment="Center">
              <TextBlock Text="{Binding CountIzin}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource StatusWarningBrush}" HorizontalAlignment="Center"/>
              <TextBlock Text="Izin" FontSize="11" Foreground="{DynamicResource StatusWarningBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
        </UniformGrid>

        <Grid ColumnDefinitions="*,*">
          <Border Grid.Column="0" Background="{DynamicResource SurfaceMutedBrush}" CornerRadius="16" Padding="10,14" Margin="0,0,6,0">
            <StackPanel Spacing="2" HorizontalAlignment="Center">
              <TextBlock Text="{Binding CountSakit}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource StatusInfoBrush}" HorizontalAlignment="Center"/>
              <TextBlock Text="Sakit" FontSize="11" Foreground="{DynamicResource StatusInfoBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
          <Border Grid.Column="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="16" Padding="10,14" Margin="6,0,0,0">
            <StackPanel Spacing="2" HorizontalAlignment="Center">
              <TextBlock Text="{Binding CountBelum}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
              <TextBlock Text="Belum" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
        </Grid>

        <!-- Sync status card -->
        <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="16" Padding="16">
          <StackPanel Spacing="10">
            <StackPanel Orientation="Horizontal" Spacing="8">
              <controls:LucideIcon Kind="{Binding SyncState, Converter={StaticResource SyncStateToIconKindConverter}}"
                                    Stroke="{Binding SyncState, Converter={StaticResource SyncStateToBrushConverter}}"
                                    Width="18" Height="18"/>
              <TextBlock Text="{Binding SyncState, StringFormat='Status: {0}'}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="{Binding LastSyncedDisplay}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <Button Command="{Binding SyncNowCommand}" Classes="primary" IsEnabled="{Binding !IsSyncing}"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Center">
              <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                <controls:LucideIcon Kind="RefreshCw" Width="16" Height="16" Stroke="White"/>
                <TextBlock Text="{Binding SyncNowLabel}" Foreground="White"/>
              </StackPanel>
            </Button>
          </StackPanel>
        </Border>

        <!-- Roster count -->
        <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="16" Padding="16">
          <Grid ColumnDefinitions="*,Auto">
            <StackPanel Grid.Column="0">
              <TextBlock Text="Total santri aktif" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
              <TextBlock Text="{Binding TotalSantri}" FontSize="22" FontWeight="Bold" Margin="0,2,0,0"/>
            </StackPanel>
            <controls:LucideIcon Grid.Column="1" Kind="Users" Width="22" Height="22" VerticalAlignment="Center"/>
          </Grid>
        </Border>

      </StackPanel>
    </ScrollViewer>

    <TextBlock Grid.Row="1" Text="Memuat..." IsVisible="{Binding IsLoading}" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
  </Grid>
</UserControl>
